import React from 'react';
import { View } from 'react-native';
import { styles } from '../ScanStyles';

export const ScanHeader: React.FC = () => {
  return (
    <View style={styles.headerContainer}>
      {/* Header spacing / status overlay */}
    </View>
  );
};
